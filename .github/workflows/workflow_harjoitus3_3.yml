name: Harjoitus3_osa3
on:
  issues:
    types: opened
permissions: 
  contents: write
jobs:
  harjoitus3_osa3:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Print opened issue info
        run: |
          echo "<br>Assignee: ${{github.event.issue.assignee.login}}" >> README.md
          echo "<br>Title: ${{github.event.issue.title}}" >> README.md
      - name: Push into git
        run: |
          git config --global user.name 'DefalDonut'
          git config --global user.email 'tsaily24@students.oamk.fi'
          git add .
          git commit -m "New issue created"
          git push
